# --- Upstream svix-server ---------------------------------------------------
# URL of the self-hosted svix-server. Inside docker-compose this is the service
# name + port; locally it is http://localhost:8071.
SVIX_SERVER_URL=http://localhost:8071

# Privileged org/admin JWT. Generate with:
#   docker compose exec svix-server svix-server jwt generate
# This token can read every tenant — it stays server-side and never reaches the
# browser.
SVIX_ADMIN_TOKEN=

# --- svix-ui session --------------------------------------------------------
# Secret used to sign operator session cookies. Use a long random string, e.g.
#   openssl rand -hex 32
SVIX_UI_SESSION_SECRET=

# Operator (admin) login credentials for the console.
SVIX_UI_OPERATOR_USERNAME=admin
SVIX_UI_OPERATOR_PASSWORD=change-me

# Public origin of svix-ui, used to build consumer App Portal magic links.
# Leave blank to use the operator's request origin automatically. Set it to your
# external URL when behind a reverse proxy. Avoid hard-coding localhost if you
# reach svix-ui from another machine — portal links would point at localhost.
SVIX_UI_PUBLIC_URL=

# Public IP that svix-server's webhook deliveries come FROM — shown on the Guide
# page so consumers know which source IP to allow through their firewall. When
# svix-server runs in Docker this is the Docker HOST's public IP (containers
# masquerade to the host), NOT the container's 172.x address. svix-ui can't
# detect it from inside a container, so set it here. Find it with
# `curl -s https://ifconfig.me` on the svix-server host.
SVIX_UI_WEBHOOK_SOURCE_IP=

# Force the Secure flag on session/portal cookies. By default svix-ui mirrors
# the connection: Secure over HTTPS (incl. behind a proxy via X-Forwarded-Proto),
# not Secure over plain HTTP — so login works on an HTTP deployment. Set to
# "true" to always require HTTPS, or "false" to never set Secure. Leave unset
# for the automatic behaviour.
# SVIX_UI_COOKIE_SECURE=true
